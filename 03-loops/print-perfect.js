// allow input from terminal
const prompt = require("prompt-sync")();


//user input
//var n = prompt("Enter an integer number ");

for (var n = parseInt(prompt("Enter an integer number ")); n > 0; n/(n-1)) {
    return n;
    console.log(n);
}